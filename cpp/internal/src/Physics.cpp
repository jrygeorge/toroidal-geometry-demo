#include <Physics.h>


glm::vec3  Physics::ResolveCuboidCollisionSAT(Cuboid X, Cuboid Y) {
	// Accepts two Cuboids and outputs the direction vector that
	// Cuboid X has to move to no longer be in collison with Cuboid Y.
	// vec3(0.0f) will be returned if the two cuboids are not currently
	// colliding.

	return glm::vec3(0.0f);
}